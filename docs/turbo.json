{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "tasks": {
        "build": {
            "inputs": ["*", "src/**", "public/**", "../package/package.json"],
            "outputs": [".vercel", ".astro", "dist"],
            "passThroughEnv": ["CROCT_APP_ID"]
        }
    }
}
